jobs:
  debug_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ایجاد دسترسی SSH به سرور لینوکسی گیت‌هاب
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
